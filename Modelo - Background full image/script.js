// Definindo as palavras usadas
var typed = new typed("span",{

    // Para definir as palavras, use o seguinte modelo: strings:["Palavra 1", "Palavra 2"],
    strings:["Landing Page Model"],
    typeSpeed: 70,
    backSpeed: 60,
    loop: true
});